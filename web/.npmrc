# Supply-chain hardening: never run install-time lifecycle scripts (postinstall,
# preinstall, prepare) from dependencies. Prevents a compromised package from
# executing arbitrary code during `npm install`/`npm ci`. Verified that a clean
# install + `npm run build` still passes with this on.
ignore-scripts=true
